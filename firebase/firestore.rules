rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Deny all client-side access - all operations go through the Backend API
    // which uses Firebase Admin SDK with Service Account credentials
    // Admin SDK bypasses Firestore Security Rules
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
